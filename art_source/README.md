# art_source

Arte em resolução de geração, antes do downscale para os tamanhos do jogo.

O `.gdignore` faz o Godot ignorar esta pasta: nada aqui é importado nem entra no
build. Os arquivos finais ficam em `resources/map/` (tiles) e
`assets/sprites/environment/` (obstáculos).
